// bt-daemon — placeholder entrypoint.
//
// This will grow into the shared daemon / event server that the coding-agent
// plugins talk to. For now it just proves the crate builds.

fn main() {
    println!("bt-daemon: placeholder");
}
