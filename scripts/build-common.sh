#!/usr/bin/env bash
# Shared helpers for per-agent build.sh (ported from sentry-for-ai).
# copy_skills <content_root> <dest>  -> rsync skills + hydrate references.yml
# copy_skill_tree <content_root> <dest>
# (placeholder)
